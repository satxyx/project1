<!-- here you should see all your  journal entries -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/nav.css">

</head>
<body>
    <%- include('../../partials/navbar.ejs') %>

    <h1>Journal Index</h1>
    <% for (let i =0; i < allEntry.length; i ++) { %>
        <a href="/entry/<%= allEntry[i]._id %>">
            <p><%= allEntry[i].title %></p>
        </a>
        <br>
        <button>
            <a href = "/entry/<%= allEntry[i]._id %>/edit">Edit Entry</a>
        </button>
        <br>
        <form action = "/entry/<%= allEntry[i]._id %>?_method=DELETE" method = "POST"> 
            <input type = "submit" value = "Delete Entry">
        </form>
    <% } %>
</body>
</html>